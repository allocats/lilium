### Builtins

All builtins are prefixed with **@**   

Example:  

```
fn cast_to_i64_from_i32(number: i32) -> i64 {
    return @cast(number, i64);
}
```

**cast**    Convert one type to another  
**sizeof**  Returns size of something 
**typeof**  Returns type of something  
